import { modelContextProtocol } from './model-context-protocol';

describe('modelContextProtocol', () => {
  it('should work', () => {
    expect(modelContextProtocol()).toEqual('model-context-protocol');
  });
});
