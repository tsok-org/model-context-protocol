# model-context-protocol

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build model-context-protocol` to build the library.

## Running unit tests

Run `nx test model-context-protocol` to execute the unit tests via [Jest](https://jestjs.io).
