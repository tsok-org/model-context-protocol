# model-context-protocol-specification

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build model-context-protocol-specification` to build the library.

## Running unit tests

Run `nx test model-context-protocol-specification` to execute the unit tests via [Jest](https://jestjs.io).
