import { modelContextProtocolOpentelemetryInstrumentation } from './model-context-protocol-opentelemetry-instrumentation';

describe('modelContextProtocolOpentelemetryInstrumentation', () => {
  it('should work', () => {
    expect(modelContextProtocolOpentelemetryInstrumentation()).toEqual(
      'model-context-protocol-opentelemetry-instrumentation'
    );
  });
});
