# model-context-protocol-opentelemetry-instrumentation

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build model-context-protocol-opentelemetry-instrumentation` to build the library.

## Running unit tests

Run `nx test model-context-protocol-opentelemetry-instrumentation` to execute the unit tests via [Jest](https://jestjs.io).
