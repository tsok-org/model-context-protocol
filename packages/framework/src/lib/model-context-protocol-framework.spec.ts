import { modelContextProtocolFramework } from './model-context-protocol-framework';

describe('modelContextProtocolFramework', () => {
  it('should work', () => {
    expect(modelContextProtocolFramework()).toEqual(
      'model-context-protocol-framework'
    );
  });
});
