# model-context-protocol-framework

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build model-context-protocol-framework` to build the library.

## Running unit tests

Run `nx test model-context-protocol-framework` to execute the unit tests via [Jest](https://jestjs.io).
