import { modelContextProtocolSdk } from './model-context-protocol-sdk';

describe('modelContextProtocolSdk', () => {
  it('should work', () => {
    expect(modelContextProtocolSdk()).toEqual('model-context-protocol-sdk');
  });
});
