# model-context-protocol-sdk

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build model-context-protocol-sdk` to build the library.

## Running unit tests

Run `nx test model-context-protocol-sdk` to execute the unit tests via [Jest](https://jestjs.io).
