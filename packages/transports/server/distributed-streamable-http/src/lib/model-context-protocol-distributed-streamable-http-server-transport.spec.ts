import { modelContextProtocolDistributedStreamableHttpServerTransport } from './model-context-protocol-distributed-streamable-http-server-transport';

describe('modelContextProtocolDistributedStreamableHttpServerTransport', () => {
  it('should work', () => {
    expect(
      modelContextProtocolDistributedStreamableHttpServerTransport()
    ).toEqual(
      'model-context-protocol-distributed-streamable-http-server-transport'
    );
  });
});
