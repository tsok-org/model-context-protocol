import { modelContextProtocolWebrtcTransport } from './model-context-protocol-webrtc-transport';

describe('modelContextProtocolWebrtcTransport', () => {
  it('should work', () => {
    expect(modelContextProtocolWebrtcTransport()).toEqual(
      'model-context-protocol-webrtc-transport'
    );
  });
});
